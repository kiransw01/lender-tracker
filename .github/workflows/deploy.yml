name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies (with retry)
        working-directory: frontend
        timeout-minutes: 5
        run: |
          npm config set fetch-retries 5
          npm config set fetch-retry-mintimeout 20000
          npm config set fetch-timeout 300000
          for i in 1 2 3; do
            timeout 180 npm install --no-audit --no-fund --prefer-offline=false && break
            echo "npm install failed/timed out (attempt $i), retrying..."
            rm -rf node_modules
            npm cache clean --force || true
            sleep 5
          done
          test -f node_modules/.bin/vite || (echo "vite binary missing after install" && exit 1)

      - name: Build
        working-directory: frontend
        run: npm run build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
